# IDOR Lab Scanner — dependências (uso educacional)
# Instale com:  pip install -r requirements.txt

flask>=3.0.0       # app-alvo vulnerável de laboratório (target_app.py)
requests>=2.31.0   # cliente HTTP usado pelo scanner
rich>=13.7.0       # TUI cinematográfica (tabelas, cores, progresso)
